server:
  port: 7001


eureka:
  client:
    # 是否将自己注册到Eureka-Server中，默认为true
    register-with-eureka: false
    # 是否从Eureka-Server中获取服务提供者的注册信息
    fetch-registry: false
    # 设置注册中心的地址
    service-url:
      defaultZone: http://localhost:${server.port}/eureka/
